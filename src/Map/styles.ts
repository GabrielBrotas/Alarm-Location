import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    map: {
        height: 300,
        width: '100%',
    }
})

export default styles